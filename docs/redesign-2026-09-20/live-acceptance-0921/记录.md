# 2026-09-21 真实开发者工具与云端验收

## 执行边界

- 当前登录的开发者工具，原配置环境 `cloud1-d4ge4gu1le8fe61d3`，未切换环境、未公开发布。
- 隔离项目 `QA隔离验收-0921-1815`，ID `ff4bc2646ab103e900915526156e60e6`。未编辑原有“11”项目。
- 使用设计演示图片 `site-detail.png`，不是真实工程证据。系统相册弹窗无法读取，因此仅选图入口使用测试替身；后续草稿、Canvas 导出、云上传、巡查确认和报告保存调用真实实现与真实云端。
- 人工核对文本通过实际页面处理函数输入；不能将其算作键盘/输入焦点的完整 UI 验收。
- 真实 AI/语音调用：本轮均为 0。未向其他微信用户发送报告。远程 PDF 服务仅使用隔离中文长文载荷完成最小生成、下载与去重验收，不含真实客户记录。

## 实际结果

| 项目 | 结果与证据 |
| --- | --- |
| 自动化连接 | 指定 `auto --auto-port 9420` 后成功；先前遗漏参数导致没有监听端口。与 `agent.skills` 无关，无须修改 app.json 添加 Agent 配置。 |
| 项目保存 | 从项目列表点击新建，填写、保存后真实云端返回项目详情。 |
| 旧后端诊断 | 第一份报告 `3618a99e6ab105df00cd26e876a65eed` 确有两条问题，但没有新版稳定关联/快照字段。下载线上 inspection 源码核实为旧实现。 |
| 标注返回 | 发现采集页原生退出提醒泄漏到标注返回；修复隐藏时关闭提醒，显示时恢复。重新编译后再次画矩形、保存、返回成功。 |
| 草稿覆盖防护 | 隐藏采集页卸载时不再将旧 form 覆盖到标注/核对页更新后的草稿；增加定向回归测试。首次标注在代码重编译期间丢失，不能作为通过证据。 |
| 新版真实报告 | `report-c133033c652d1663ea91c856f26b1b9365332e2f`，snapshotVersion=2，1 张照片、2 条问题、每条均关联同一照片与一处矩形标注。 |
| 报告重新打开 | 离开报告进入报告列表，再按真实 ID 打开；问题 ID、来源照片 ID、文字、标注坐标逐项与保存前相同。 |
| 照片对应 | 照片 ID `1789986539002-0.951550886866706`；问题 ID `issue-mub3su3y-2t8drg1ptq`、`issue-mub3tbzi-lyfau58jkr`。 |
| 报告底栏 | 改成一行“转发报告 + PDF”；管理分享、保存到本地、返回入口移到正文末尾；取消错误信息截断。已获取真实模拟器截图。 |
| 远程 PDF | 服务器内新版服务已切换，鉴权拒绝、异步生成、稳定任务 ID、PDF 下载与公开预览接口的隔离验收通过；公网 HTTPS 证书过期，云函数经域名调用及微信端下载仍未通过。 |
| 服务器登录 | 按现有部署资料对 `ubuntu@49.232.215.168` 做非交互 SSH 检查，返回 Permission denied (publickey,password)。未修改服务器配置，等待用户提供已登录的控制台远程连接。 |

## 已部署及回退资料

`project`、`inspection`、`report`、`ai` 云函数已在原环境部署成功。部署前下载的线上版本存于 `/Users/cft/.codex/backups/haoli-20260921/`，未放入仓库，避免包含运行配置。AI 仅更新任务可靠性代码，未发起模型调用。新版客户端未上传体验版，未提交审核。

本次线上报告证明兼容后端接受了新版数据，不代表幂等故障注入、跨账号鉴权与分享、全部历史记录兼容已在真云端通过。

## 本轮定向本地复核

- 原生模板编译：24 WXML、27 WXSS，JS/JSON 检查通过。
- 数据/云函数/恢复相关 10 个隔离测试通过；AI 任务 2 个隔离测试通过。
- 这些是本地测试，不替代真云端故障注入或模型调用。

## 尚未完成

公网 HTTPS 与云函数到 PDF 服务的真实调用、真实 AI/语音、分享接收方/撤销后读取、20 张照片及旋转图/大图、重编译中断回归、iOS/Android 真机、体验版上传。体验版仍不宣称验收通过。

补充：修复微信 style:v2 默认按钮宽度覆盖导致的导航偏移与主按钮变窄，重新编译后的导航截图已核对。原“11”项目仍显示 2 次巡查、9 条问题；本轮未修改该记录。

截图：`haoli-live-v2-report.png`、`haoli-live-reopened-report.png`、`haoli-live-annotation-return.png`、`haoli-live-nav-fixed.png`。

## 19:38 服务器诊断更新

- 已通过用户登录的 OrcaTerm 进入 `ubuntu@VM-0-15-ubuntu`；未修改同机其他服务。
- PDF 进程 online；本机 3100 `/health` 返回 success:true；Nginx 配置测试通过。
- 本机使用正确域名/SNI 请求 HTTPS 返回 certificate has expired。Certbot 显示原证书到期时间为 2026-07-06 11:54:30 UTC。
- 对现有域名执行一次非交互续期，失败：CA 域名验证收到 `dnspod.qcloud.com/static/webblock.html?d=pdf.haolizhiguan.cn` 拦截页面；未重复申请、未关闭 TLS 校验、未更换 DNS 或服务器。
- 腾讯云备案控制台明确显示 `haolizhiguan.cn` 为“未备案”；注册控制台显示域名服务正常。购买域名不等于完成备案。
- 原有 certbot.timer 为 enabled/active，但自动续期存在并不能解决域名验证拦截。公网证书仍未续期成功；后续已在服务端完成新版受控切换，详见下文。
- 域名关口：用户已明确先按个人主体推进，不再重复询问主体。备案所需材料与核验尚未办理；未代填或提交。这不阻止服务器内部生成验收及其余产品工作。

## 20:15 远程 PDF 独立生成与部署预检

- 本机完整隔离回归重新运行：23 项全部通过；不替代云端和真机验收。
- 恢复原实例 `lhins-odzyvdc2` 的 OrcaTerm 免密连接；确认主机 `VM-0-15-ubuntu`、PM2 名称 `report-pdf-service`、入口 `/home/ubuntu/report-pdf-service/server.js`。同机 `course-note` 未修改。
- 已上传 `/home/ubuntu/haoli-pdf-A-20260921.tgz`；远程 SHA256 与本机一致：`d09f4028a374c1eee51a32fe4809548e2481dd83c5029ff2ca208e8dbbe33e87`。
- 部署预检发现 PM2 中未提供新版要求的 `PDF_API_KEY`，第一次检查在任何覆盖或重启前中止。不能从这个事实推断旧服务完全没有其他鉴权方式，也不能直接部署会因缺密钥拒绝启动的新版本。
- 已完成旧服务文件备份（不含既有 node_modules）及 PM2 配置备份，路径 `/home/ubuntu/haoli-pdf-backup-MkhmVj`，目录权限 0700、PM2 配置 0600。配置仅留服务器，不写 Git 或在输出中展示。
- 新版解压至独立目录 `/home/ubuntu/haoli-pdf-stage-dd8AC6`，复用现有依赖，没有安装软件或改动现有服务目录。
- 通过新版真实 Puppeteer 渲染器，在该服务器生成 `/home/ubuntu/haoli-pdf-backup-MkhmVj/staging-sample.pdf`，206921 字节，文件头 `%PDF-` 验证通过。输入为明确标记的隔离中文长文样本，不含照片、不是真实客户报告。
- 浏览器文件传输页显示传输成功，但 Chrome 下载结果提示“需有下载权限”，本机未找到文件。因此中文可读性、实际分页视觉、照片关联均保持未验证；不把传输页成功当作文件交付成功。
- 该预检阶段运行中的旧服务未覆盖、未重启；正式异步接口、鉴权拒绝、重复任务与下载验收当时尚未执行。后续切换与验收结果见下节。

## 远程 PDF 受控切换与最小接口验收

- 用户已在 `report` 云函数配置同名 `PDF_API_KEY`；服务器配置保存在 `/home/ubuntu/.config/haoli/report-pdf-service.env`，权限 0600。验收只确认文件存在、非空、权限正确，未读取或输出密钥。
- 已将新版包解压覆盖到既有 `report-pdf-service`，仅重启该 PM2 进程并执行 `pm2 save`；同机 `course-note` 未修改。PM2 显示 `report-pdf-service` online，TCP 3100 端口连通。
- 运行 `/home/ubuntu/haoli-deploy-pdf-v2.cjs` 通过：健康检查、未带密钥返回 401、相同请求返回稳定 jobId、异步任务成功、PDF 下载、公开预览及服务版本切换检查均通过。隔离 PDF 为 206921 字节。
- 未改动 DNS、证书或防火墙。`pdf.haolizhiguan.cn` 的过期 HTTPS 证书仍阻断云函数按公网域名请求；因此不把服务器内验收当作小程序“生成 PDF”全链路通过。
- 下一步：恢复可验证的公网 HTTPS 后，使用隔离巡查记录从 report 云函数发起一次最小任务，核对云存储照片、在线报告与实际 PDF 的逐项对应，再做真机验收。

实施脚本：`tests/deploy-pdf-remote.cjs`。包含固定主机/路径/部署包校验、旧服务备份、独立渲染、缺密钥阻止切换，以及切换后失败回退。不得绕过缺密钥检查。
