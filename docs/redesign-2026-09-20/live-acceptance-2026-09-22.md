# 2026-09-22 全流程与界面收敛验收

## 本次范围与边界

- Checkout：`0896477` 加当前未提交改动；开发者工具：Stable `2.02.2608060`；AppID：`wxf30ba9025bea84ab`；当前云环境在开发者工具中显示为 `cloud1`。
- 测试对象：隔离项目 `QA-0922-B`。只写入该项目；不修改“11”、历史报告或真实客户媒体。
- 测试照片：仓库中的设计演示图片；不使用客户照片。测试产生的云端媒体、巡查和报告均标记为隔离验收数据。
- 不向其他微信用户发送报告。仅验证当前报告是否生成独立分享令牌；接收方读取与撤销需独立账号。
- AI、语音和公网 PDF 先核实配置/计费/HTTPS。未核实前只走纯手动核对，不把跳过 AI 当作 AI 通过。

## 代表性用例

| 步骤 | 预期合同 | 状态 |
| --- | --- | --- |
| 1. 身份与项目 | 已登录；项目列表显示 QA-0922-B；进入后不跳转到其他项目 | 通过（开发者工具）：项目行已成为原生可点击语义按钮；实际进入 QA-0922-B，详情页项目名、草稿归属和后续页面始终未被其他项目覆盖。 |
| 2. 采集 | 当前项目明确；演示图片持久保存；退出重进仍在同一会话 | 通过（开发者工具）：从项目详情重开本机草稿，恢复 QA-0922-B、1 张照片和原会话；生成报告后该草稿按成功提交规则清理，项目统计更新为 1 次记录、1 份报告。 |
| 3. 标注 | 在同一图片绘制至少一个矩形和一个编号点；保存返回后仍可编辑 | 部分通过：冷重开后矩形与“已标注 1 处”真实恢复；再次进入全屏标注可选中、右移微调、撤销状态更新并保存。矩形的预览与报告位置一致；编号点和真机双指仍待补验。 |
| 4. 人工核对 | 手动新增一个问题并填写文字；照片可零问题保留；当前项目不变 | 通过（本用例）：从采集直接进入人工核对，手动新增并完成“窗台右侧收口不平整，需补胶处理。”及建议；页面保持 QA-0922-B。零问题保留路径已由自动化覆盖，仍待真机交互补验。 |
| 5. 报告 | 保存后生成一份报告；重开时照片、标注、问题编号和文字一一对应 | 通过（开发者工具）：生成 `HLZG-20260922-4164` 后返回项目详情，再从“最近报告”和报告一级页重开；项目、1 张标注照片、“问题 一 / 1.”、问题描述和建议完全一致。 |
| 6. 分享 | 当前报告才可生成/管理分享；不跳转报告列表 | 部分通过：报告发布即生成当前报告专用随机令牌，页面显示“转发这份报告”和“分享设置”；自动化验证只读白名单、撤销失效与快照冻结。未向第三方发送，独立微信账号读取仍待补验。 |
| 7. PDF | 在线报告不因 PDF 失败阻断；公网 HTTPS/PDF 结果单独记录 | 待实测 |
| 8. UI 收敛 | 项目、记录、标注、核对、报告、我的均有单一主操作、正确身份层级、无无意义常亮状态或大块缝隙 | 通过（开发者工具当前机型）：实际走过项目列表、项目详情、采集、全屏标注、核对、报告详情、报告列表和“我的”。全部使用连续暖纸面、细分隔与单一主操作；三栏导航状态同步；项目统计改为单行避免断字；“我的”具备完整加载骨架及公司/LOGO/巡查人/电话状态。真机安全区与 Android 字体仍待补验。 |

## 停止条件

出现跨项目错配、草稿丢失、照片/标注/问题错配、重复报告、越权或无法重开报告时立即停止，不再扩大测试。开发者工具热重载造成的旧 Webview 截图与实际语义树不一致时，保留为工具限制，不把它写成产品通过或失败。

## 本轮复核结果（16:41）

- 已完成真实闭环：QA-0922-B 从草稿冷重开、标注恢复与微调、人工新增问题、确认发布，到项目详情和报告一级页两次重开均通过。只写入该隔离项目，没有触碰其他项目。
- 报告复核：`HLZG-20260922-4164` 固定显示 QA-0922-B、1 组照片、1 条问题；红色矩形位置、`问题 一 / 1.`、问题描述和建议在重开后保持一致。
- 全局界面复核：项目卡已改为完整按钮语义；项目统计改为一行“次数 · 问题数 + 日期”，消除窄屏断字；底栏当前项用中性墨黑图标、文字和朱橙短线表达；“我的”加载改为完整骨架，避免先出现零散设置再跳变。
- 自动复核：31 项测试通过；`wcc` 26 个模板、`wcsc` 29 个样式与 JavaScript/JSON/原生模板检查通过；补丁空白检查通过。全新编译后的开发者工具控制台为 0 error、4 个环境/性能类 warning，旧标注 WXSS 选择器 warning 已消失。
- 当前未验证：编号点和真机双指标注、独立接收账号读取与撤销、PDF 公网 HTTPS、AI、语音，以及 iOS/Android 真机安全区和键盘。AI/语音/PDF 配置与计费未核实，因此本轮没有发起真实调用；也没有发送任何真实转发。
- 资料路径：当前账号资料为空，“我的”显示 `1 / 4`，报告快照如实显示“未填写公司名称”和巡查员；资料设置已提供巡查人、巡查电话、公司 LOGO、公司名称、公司电话、公司地址。测试没有虚构或代填用户公司信息。

## 全局视觉与返回链路复核（18:35）

- 已纠正确认稿落实偏差：暖白 `#F5F1E9` 是页面、导航和工具栏的唯一大面积底色；正文与底栏当前项统一为中性墨黑 `#1C1A17`，普通照片预览的比例留白使用暖灰 `#ECE6DC`，仅全屏精准标注工作区使用中性炭黑 `#1A1917`。深青 `#143D38` 不再承担正文、链接、页面底色或选中态，只保留在品牌图形内部。
- 已统一双字体规则：`Songti SC / STSong / Source Han Serif SC / Noto Serif CJK SC` 以 `500` 字重用于品牌名、页面主标题、项目名、报告名、问题分组与报告出具单位；`PingFang SC / Noto Sans CJK SC` 用于正文、表单、按钮、导航、编号和时间。所有原生按钮、输入框和文本域显式继承正文字体，避免组件回退成另一套系统字体。
- 标注工具已换成同一 24×24 光学网格、`1.7` 线宽的本地 SVG 资产；主绘制工具保留名称，撤销、重做、缩放、适应和删除形成统一次级工具列，选中态只使用朱橙线与短指示条，不再使用大面积色块。选中标记后的微调已改为一处“微调位置”标签加四个 44px 图标按钮。
- 在 QA-0922-B 草稿中实际复核矩形与编号点恢复、撤销、重做、选中微调和保存。保存标注后直接返回记录页，标注数量更新为 3；不再出现保存成功后的二次返回确认。
- 继续回退时又复现系统级返回提示会残留到项目详情。现已取消原生跨页拦截，改由“每次编辑即保存 + 页面隐藏/卸载再保存 + 页面自身返回按钮”保证草稿；对应回归测试已覆盖，避免确认框泄漏到下一页。
- 公司 LOGO 上传成功后现在立即写入报告资料并显示“LOGO 已保存”，不再依赖用户再找一次底部保存按钮；若资料保存失败，会恢复旧预览并明确提示，不显示成已完成。
- 当前自动复核新增覆盖：三栏导航回到当前页面时同步选中、重复点击当前栏不重复跳转；全局暖纸面/双字体/标注工具栏不使用深色背景；LOGO 上传即时保存；标注返回不安装跨页系统拦截。
- 全量回归结果：35/35 自动测试通过；WXML 26 个文件、WXSS 29 个文件及 JavaScript/JSON/原生模板检查通过；补丁空白检查通过。随后在开发者工具实际执行“项目列表 → QA-0922-B → 继续草稿 → 返回项目详情 → 返回项目列表”，两次返回均无多余系统确认，草稿仍显示 1 张照片。

## 确认稿色彩与字体二次收口（19:17）

- 发现旧 A 方案仍把深青同时用作正文、链接、图标和照片比例留白，虽然页面已经变浅，实际观感仍会残留“墨绿底”和多套视觉语言；这与最终确认的暖纸面参考不一致。
- 已将正文、通用图标与底栏当前项统一为中性墨黑 `#1C1A17`，普通照片预览的比例留白改为暖灰 `#ECE6DC`；深青 `#143D38` 只保留给 v8 品牌图形，朱橙 `#E15D3A` 继续承担主操作及标注选中态。
- 字体规则进一步闭合：展示宋体统一为 `500` 字重；按钮、输入框、文本域显式使用同一无衬线正文字体；清除了项目、报告、核对、欢迎和资料页中各自覆盖的展示字重差异。
- 在开发者工具重新走过项目列表、QA-0922-B 项目详情、现场记录和精准标注。项目页与记录页没有深青大底；现场照片两侧不再出现墨绿色块；精准标注仅保留中性炭黑照片工作区，工具栏仍为暖纸面，朱橙选中态清楚。
- 修正现场记录页的标签选择器警告后清空控制台重测，当前开发者工具显示 `Errors: 0, Warnings: 0`。这只说明本次页面运行没有调试错误，不替代真机与业务验收。

## 报告、账号与权限恢复复核（19:27）

- 在开发者工具继续实际打开报告列表、`QA-0922-B` 单份报告、“我的”和报告资料设置。四页均保持暖纸底、中性墨字、朱橙主操作；报告照片与表单区域没有再出现墨绿底。空头像也改为中性暖灰，不再把品牌深青扩散到普通对象。
- 报告中的“一般”严重度由浅绿底改为暖灰底。至此深青 `#143D38` 在正式界面中不再用于正文、普通状态、卡片、内容背景或底栏，只存在于品牌资产内部。
- 旧报告没有公司资料时改为如实显示“个人出具”，不再在交付封面突出“未填写公司名称”；新报告仍会冻结公司名称、LOGO、巡查人、出具人和联系电话，已发布报告不跟随后续资料变化。
- 修正相机/相册拒绝与主动取消被混为一类的问题：主动取消保持安静；相机拒绝可直接转到相册；相册拒绝可打开设置；更换照片失败时明确说明当前照片、标注和文字仍保留。麦克风拒绝提示同步明确“可直接输入文字”。
- 自动回归新增欢迎页初始化失败重试、照片权限拒绝恢复、麦克风拒绝文字替代以及视觉品牌色边界，共 `38/38` 项通过；WXML 26 个、WXSS 29 个编译通过，JavaScript/JSON/原生模板检查和补丁空白检查通过。
- 仍未宣称通过的项目：独立微信接收者实际打开与撤销、iOS/Android 真机手势和键盘、公网 HTTPS/PDF、真实 AI 与真实语音。开发者工具本轮累积的 warning 为基础库、预加载、热重载和 worker 能力提示，不是本次页面语法或业务错误；需在最终回归时清空后重新记录。

## PDF 视觉同源复核（19:42）

- 发现生产 PDF 模板仍残留旧 A 方案的 `#232920` 深绿正文、`#D6DACF` 冷灰分隔和 `#F0F2EA` 照片底，在线报告与下载件并非同一视觉系统。现已统一为暖纸 `#F5F1E9`、中性墨 `#1C1A17`、暖灰 `#ECE6DC`、分隔 `#D8D0C4` 与朱橙 `#E15D3A`。
- PDF 标题与“问题 一 / 二 / 三”采用与小程序一致的展示宋体栈，正文、日期、编号和元数据使用无衬线栈；长问题正文降为正常字重，避免整页粗体造成阅读压迫。
- PDF 出具信息支持公司名称、LOGO、公司电话、地址、巡查人、出具人及双方电话；无公司资料时如实显示“个人出具”。产品对外文案不再使用“证据”，统一为“现场记录 / 现场照片 / 现场位置”。
- PDF 模板版本提升为 `warm-editorial-v3`。已有旧版 PDF 会显示需要重新生成，但在线报告不受影响；重试仍从已冻结报告快照生成，不会读取后来变化的资料。
- 已使用隔离图片离线生成 4 页 A4 样本并逐页渲染检查：照片完整、问题分组顺序正确、零问题照片保留、超长文字自然跨页、页码完整，无裁切、重叠、黑块或旧墨绿色。样本只用于版式验证，不代表公网 PDF 服务已恢复。

## 母版与正式运行态一致性复核（20:06）

- 修正交互母版残留的旧 A 规则：移除项目与公司资料的灰色圆角卡片、墨绿返回箭头和栏目提示、整块橙色工具选中态；统一为透明连续内容、暖灰细分隔和朱橙短指示线。
- 母版与正式小程序现在共用同一字体角色：展示宋体只用于品牌、页面标题、项目／报告名称和问题分组，并固定 `500` 字重；正文、表单、按钮、导航、编号和时间使用系统无衬线。双字体是信息层级，不再由各页面自行选择字体。
- 在开发者工具实际进入 `QA-0922-B → 未完成记录 → 点击照片进入精准标注`。标注工具栏为暖纸底、统一黑色线性图标；当前“选择”只显示朱橙文字与短线。画布周围的深色仅为中性炭黑照片工作区，不是墨绿页面背景。
- 同时重开项目详情与单份报告：页面均为暖纸底、中性墨字、朱橙主操作；深墨绿没有出现在内容背景。报告显示“个人出具”、巡查人和报告出具人，字体角色与项目页一致。
- 母版交互测试新增暖纸色、无卡片填充、宋体标题字重和工具选中态断言；320／375／430 三种宽度无横向溢出。全量回归仍为 `38/38`，WXML/WXSS 编译、JavaScript/JSON/原生模板检查及补丁空白检查通过。

## 首次接收分享与兼容色值复核（20:18）

- 检查首次打开单份报告的调用链：带有效 `shareToken` 的报告读取不会触发作者端 `ensureReady`，因此没有使用过小程序的接收者不会被“开始使用／创建项目”流程拦住；普通拥有者读取仍必须完成身份初始化。
- 新增客户端回归，分别证明“首次接收者可直接只读打开”和“无分享令牌的拥有者读取仍受身份保护”。云端既有回归继续覆盖收件人只读字段白名单、撤销旧链接失效和报告快照冻结。
- 清理旧兼容变量中的残留绿色活动态与旧紫灰阴影；旧组件即使误用兼容 token，也只能得到中性墨黑、暖灰和当前朱橙，不会重新引入第二套主题。
- 全量自动回归更新为 `39/39` 通过；WXML 26 个、WXSS 29 个编译通过，JavaScript/JSON/原生模板检查和补丁空白检查通过。独立微信账号的真实接收与撤销仍需第二账号实测，当前自动化不能替代该外部身份验收。

## 自定义组件字体隔离复核（20:31）

- 复核确认稿时发现，小程序页面虽然定义了正文字体，但自定义组件存在样式隔离，标注工具、项目行、空态、顶部导航、底栏、资料上传和报告段落可能退回各自的原生字体；这正是“同一页像几套字体”的实际来源。
- 已把系统无衬线字体直接写入全部公共组件根节点和原生按钮；只有品牌名、页面标题、项目／报告名称、问题分组与空态标题使用展示宋体 `500`。字体差异现在只表达信息层级，不再由组件边界随机决定。
- 深墨绿背景禁用规则升级为全量样式扫描：任何正式 WXSS 都不得使用 `var(--a-brand)` 作为背景。深墨绿只允许用于 v8 品牌图形内部；精准标注照片四周是中性炭黑 `#1A1917` 工作区，不属于页面主题背景。

### 核对与报告层级复核（21:17）

- 人工核对页已改为导航标题“人工核对”＋页面任务“确认内容”，移除重复措辞、装饰图标和固定底栏中的二次说明，只保留一个主操作。
- 报告页首屏已移除重复的“已确认／问题数／照片组”状态行，结论改为人工语义“本次已确认 N 项问题”。
- 报告交付区已调整为“转发这份报告”为主操作，PDF 为紧凑次操作；底部安全留白同步增加，避免遮挡报告内容。
- 微信开发者工具 iPhone 15 Pro Max 模拟器已实际打开项目详情、现场记录、人工核对与已发布报告；四页均为暖纸底＋中性墨字＋朱橙主操作，未见墨绿界面底色。
- 字体角色锁定：品牌名、页面标题、项目名、报告标题和“问题 一”使用同一展示宋体栈；导航、正文、表单、按钮、数字和日期使用同一系统黑体栈。不是每页自由选字体。
- 在开发者工具重新查看项目、项目详情、现场记录、精准标注、报告和“我的”：页面与工具栏均为暖纸底，正文与普通图标为中性墨黑，主操作为朱橙；自定义组件内按钮和文字的字体角色一致。开发者工具本次运行无业务错误，现有提示为基础库、预加载和热重载类环境信息。
- 本地 PDF 服务补做真实进程联调：无密钥请求返回 `401`；同一固定任务 ID 连续提交只创建一个任务；任务从排队／运行进入成功；下载响应为 `application/pdf` 且文件头、字节数与任务状态一致。测试只使用本机隔离资料，没有访问公网或上传数据。
- 全量回归更新为 `40/40` 通过；WXML 26 个、WXSS 29 个编译通过，JavaScript/JSON/原生模板检查和补丁空白检查通过。公网 HTTPS 与微信端下载仍是独立外部验收项，本地通过不能替代它们。

## 底栏色彩、字体与标注语义复核（20:54）

- 最终取消底栏选中图标的深墨绿色：三个一级导航图标统一为相同线宽的中性墨黑轮廓，当前项只由深墨文字和朱橙短线表达。深墨绿现在仅存在于 v8 品牌图形内部，不再作为任何界面控件的底色或选中颜色。
- 公共组件的字体回退栈已与页面级 token 完全一致，包含同一组中英文系统无衬线候选；品牌名、页面标题、项目／报告名称和问题分组仍统一使用展示宋体 `500`。真实开发者工具热更新后复核项目列表和项目详情，暖纸底、标题与正文角色一致。
- 标注语义收敛：矩形、圆形和箭头只负责圈选／指向，不再自动叠加编号；只有“编号”工具生成可见编号点。编号显示按当前编号点顺序连续计算，删除后不会留下视觉跳号，稳定标注 ID 不变。
- 在 `QA-0922-B` 草稿中重新打开精准标注，已保存的矩形不再显示数字，编号点显示连续编号；没有点击保存，因此未改写该隔离草稿。核对页产品文案同步将“AI 记忆／AI 依据”改为“历史提示／识别参考”，保持“AI 只提供参考、人工确认结果”的产品定位。
- 全量回归更新为 `41/41` 通过；WXML 26 个、WXSS 29 个编译通过，JavaScript/JSON/原生模板检查和补丁空白检查通过。开发者工具当前没有业务错误；基础库、预加载、热重载和 worker 能力提示仍按环境信息记录，不作为业务通过证明。

## 资料维护与零问题报告重开复核（21:59）

### Product Design 审核步骤

| 步骤 | 实际操作 | 健康度 |
| --- | --- | --- |
| 1 | 项目一级页：检查产品识别、搜索、项目列表、主操作与三栏导航 | 通过：暖纸底、中性墨字、朱橙主操作；无墨绿界面底色。 |
| 2 | “我的”：检查身份、公司资料、LOGO 状态、说明与协议入口 | 通过：资料缺失如实显示，长名称和状态可换行，不用默认“微信用户”掩盖失败。 |
| 3 | 报告资料：从“报告抬头”进入，检查巡查人、LOGO、公司字段和保存 | 通过：取消重复大标题；加载、失败和表单互斥；LOGO 权限拒绝有“去设置／暂不上传”。 |
| 4 | 新建项目：检查标题、必要字段、选填折叠和唯一主操作 | 通过：导航只显示“新建项目”，内容区不再重复标题。 |
| 5 | QA-0922-B 项目详情：继续已有草稿 | 通过：始终保持 QA-0922-B 归属，没有切换到列表第一项或其他项目。 |
| 6 | 现场记录：检查照片、标注入口、录音、手动核对与可选 AI | 通过：主操作为“进入人工核对”，AI 退为次级选项；照片和项目先于辅助输入。 |
| 7 | 精准标注：打开已保存照片，检查工作区、工具、状态与保存 | 通过：中性炭黑仅用于照片工作区；工具栏为暖纸底；同一线性图标系统；朱橙只指示当前工具。 |
| 8 | 人工核对：以 1 张照片、0 条问题生成报告 | 通过：照片保留，并明确“未记录问题不代表验收合格”；页面只有一个生成主操作。 |
| 9 | 报告交付与重开：生成后返回项目详情，再打开最近报告 | 通过：生成 `HLZG-20260922-677B`；项目统计更新为 2 次记录、2 份报告；重开后项目、照片、3 处标注、0 条问题、巡查人和报告编号一致。 |

### 本轮代码与回归

- 报告资料页增加未保存返回拦截；LOGO 上传只立即保存已确认的公司资料与新 LOGO，不会顺带提交页面上其他尚未保存字段。
- 相册或相机权限拒绝时提供恢复入口；用户也可暂不上传 LOGO，继续保存其他资料。
- 资料页和新建项目页取消导航标题与内容大标题的重复；资料加载时不再同时露出空表单。
- 自动回归 `43/43` 通过，其中本地 PDF 服务真实联调包含鉴权、任务去重、完成与下载；WXML 26 个、WXSS 29 个及 JavaScript/JSON/原生模板检查通过；补丁空白检查通过。

### 证据边界

- 截图与可访问性树来自微信开发者工具 Stable `2.02.2608060`、iPhone 15 Pro Max 模拟器，证明当前模拟器页面结构和视觉状态，不替代 iOS/Android 真机触控、字体、安全区与键盘验收。
- 本轮报告只写入隔离项目 QA-0922-B，没有转发给第三方，也没有触发公网 PDF、真实 AI 或真实语音调用。
- 独立接收账号读取／撤销、公网 HTTPS PDF 和双平台真机仍是外部条件项，不以模拟器结果冒充通过。

## 色彩与字体最终约束复核（22:38）

- 用户再次指出旧墨绿与字体不一致后，重新核对正式运行态与源文件。当前项目一级页实际显示为暖纸底 `#F5F1E9`、中性墨字 `#1C1A17`、朱橙主操作 `#E15D3A`；底栏没有墨绿底或墨绿选中块。深墨绿只留在 v8 几何品牌图形内部。
- 产品识别文案统一为“现场记录 · 精准标注 · 清楚交付”，欢迎页、一级页和报告签名不再出现不同版本的能力说明。
- 字体规则固化为两种角色：展示宋体只用于品牌、页面／项目／报告／问题分组标题；系统黑体用于正文、导航、按钮、表单、数字和时间。公共组件的展示字体增加完整回退栈，避免组件隔离时随机退回另一套字体。
- 报告详情与人工核对页已删除旧版和新版互相覆盖的样式，改为各自一份最终规则；报告封面、问题正文、出具信息、分享和 PDF 操作不再由加载顺序决定字号或高度。
- 自动视觉合同新增三道阻断：正式 WXSS 出现旧深青色即失败；出现第三套字体声明即失败；出现小于 24rpx 的辅助字即失败。全量回归 `43/43` 通过，WXML 26 个、WXSS 29 个及 JavaScript/JSON/原生模板检查通过，补丁空白检查通过。
- 开发者工具清空后不再出现页面 WXSS 选择器告警；当前控制台中的 `DOMNodeRemoved` 来自 Stable `2.02.2608060` 自带调试渲染层，并非小程序页面代码。模拟器截图只能证明当前机型运行态，Android／iOS 真机字体仍需设备验收。

## 暖纸界面与字体角色最终纠偏（23:28）

- 重新以自动化方式从正式小程序获取运行态截图和计算样式，而不是只看 HTML 母版。项目一级页、QA-0922-B 项目详情、现场记录、已发布报告和“我的”均实测为暖纸背景 `rgb(245, 241, 233)`、中性墨正文 `rgb(28, 26, 23)`；正式界面没有墨绿底色。深青仍只存在于 v8 Logo 图形内部。
- 找到并修正了真实的字体层级错误：报告中的“巡查小结／问题清单”等同级栏目标题原来仍是粗黑体，而整句报告结论反而用了宋体。现在栏目标题统一为展示宋体 `500`，报告结论与连续正文统一为系统黑体 `400`；按钮、导航、数字与日期继续只使用系统黑体。
- 现场记录页同步把“现场照片／现场补充”收进同一标题角色；“本次记录归属”、说明、数量、按钮和“更换项目”保持系统黑体，不再由页面各自决定字体。
- 删除了空状态里在暖纸背景上不可见的白色加号图标，并把空状态垂直留白从 `88rpx` 收到 `56rpx`。这同时修复了“图标像消失”和首屏空态占位过大的问题；空态不再使用无业务意义的装饰图标。
- 本轮运行截图：`output/runtime-visual-audit/01-project-list.png`、`02-project-detail-final.png`、`03-report-detail-final.png`、`04-capture-final-v2.png`、`04-profile.png`、`05-annotation.png`、`06-review.png`。运行态计算结果中，页面为系统黑体，标题为 Songti 字体栈 `500`，主按钮为朱橙 `rgb(225, 93, 58)`。精准标注画布使用中性炭黑工作底，工具栏仍为暖纸底；当前工具只以朱橙图标与文字表达。
- 全量自动回归更新为 `43/43` 通过；WXML 26 个、WXSS 29 个、JavaScript/JSON/原生模板检查及补丁空白检查通过。截图来自开发者工具 iPhone 15 Pro Max 模拟器，不能替代 Android 字体回退和真机安全区验收。

## 草稿中断与重开恢复复核（23:56）

- 仅使用隔离项目 `QA-0922-B` 的草稿 `inspection-create-muctvi9y-8hbxyfwc2h`：在人工核对页为照片 `visual-audit-photo` 新增 1 条问题，并填写描述“窗框右侧收口不平整，需补胶处理。”、建议“清理基层后重新打胶，并复核连续性。”和部位“窗框右侧”。
- 从人工核对退出到项目一级页，再通过同一 `sessionKey` 重开。实际恢复结果为项目 ID `project-50768a216f7e6b9f972d132a2b1df6d4fbc21643`、项目名 `QA-0922-B`、照片 ID `visual-audit-photo`、来源照片云路径和 1 条问题内容全部一致；问题继续按稳定 `sourcePhotoId` 指向该照片，没有依赖数组位置或临时路径。
- 再回到现场记录页，项目 ID、项目名称、草稿会话、照片数量、照片 ID 和云端图片引用仍一致。该次检查证明当前本机草稿在“核对编辑 → 退出 → 重开 → 返回采集”范围内可恢复，未发现串到其他项目或丢失文字／照片的情况。
- 运行态截图：`output/runtime-visual-audit/07-review-reopened.png` 与 `08-capture-reopened.png`。前者证明暖纸核对页和同一项目／问题数量恢复；后者证明同一项目下 1 张照片继续存在。字段精确值由自动化读取页面数据断言，截图受首屏高度限制，不能单独证明折叠区域内的全部文字。
- 本轮没有提交或创建第二份报告，没有调用公网 PDF、真实 AI、真实语音，也没有向第三方发送分享链接。

## 现场记录操作层级收敛（次日 00:08）

- 运行态复核发现：已有照片后仍保留整幅朱橙“拍照或添加照片”，会与页面底部“进入人工核对”争夺主操作；单张照片还显示两个必然禁用的排序按钮。现已改为无照片时显示大入口，有照片后只在数量旁显示“继续添加”；单张照片完全不显示排序，多张照片才出现紧凑的上移／下移文字操作。
- 删除项目归属行左侧无业务含义的朱橙装饰线。朱橙继续只服务拍摄空态、人工核对、保存与分享等当前主操作，不作为普通信息强调。
- 交互母版同步移除采集页中与正式实现不一致的“选择／矩形标注／精细编辑”三按钮；改为点击照片进入精准标注，并与正式页面保持“继续添加、按住说话、文字补充、底部人工核对”的同一操作顺序。
- 新运行截图 `output/runtime-visual-audit/09-capture-converged.png` 显示 `QA-0922-B` 下 1 张照片时，首屏只保留一个底部朱橙主操作；照片添加、替换、标注、语音和文字仍可直接到达。
- 全量自动回归 `43/43` 通过；WXML 26 个、WXSS 29 个及 JavaScript／JSON／原生模板检查通过；补丁空白检查通过。

## 20 张照片、长文字与跨项目运行复核（2026-09-23 00:15）

- 在 `QA-0922-B` 下创建独立本机压力草稿 `inspection-create-qa20-20260923`，写入 20 个稳定照片 ID、1260 字现场文字及同一张隔离云图片引用。将第 20 张上移后退出到项目页，再通过同一会话重开；20 张数量、项目归属、首张 ID、18／19 位排序和长文字长度全部保持一致。
- 运行截图暴露两个界面问题并已修复：到达 `20 / 20` 后不再显示“继续添加”；可选 AI 入口移出吸底操作区，吸底区域只保留“进入人工核对”，并补足安全区和页面底部空间，减少长列表被操作区覆盖的面积。
- 修复后截图：`output/runtime-visual-audit/12-qa20-limit-top-fixed.png`、`13-qa20-long-text-fixed.png`、`14-qa20-bottom-fixed.png`。截图确认 20 张上限状态、连续问题分组、长文字自然换行、底部资料字段与主操作均可到达；同一图片重复引用不能替代 20 张不同大图的真机内存和上传耗时测试。
- 继续用独立空会话进入 `QA隔离验收-0921-1815`，页面为 0 张照片；随后重开 `QA-0922-B` 原核对草稿，仍恢复 1 张照片、1 条问题，照片 ID 与问题 `sourcePhotoId` 均为 `visual-audit-photo`。截图为 `15-cross-project-empty.png` 和 `16-cross-project-original-resumed.png`。
- 20 张压力草稿和跨项目空会话完成取证后已精确从本地存储移除；原 `inspection-create-muctvi9y-8hbxyfwc2h` 草稿、历史记录和报告均保留。没有提交压力记录、上传新图片或调用外部服务。

## 单报告分享与零问题照片表达复核（2026-09-23 00:27）

- 对已发布隔离报告 `report-deed1bdefd69fedfe1da84606f3085a9d1b9677b` 只读调用微信分享配置，没有触发转发。结果只指向 `/pages/report/detail/index`，查询参数仅包含该 `reportId` 与报告专属 `shareToken`，没有报告列表、项目入口或其他内部参数；分享状态为 active，卡片图片为空或 HTTPS 地址。
- 运行截图 `17-single-report-share-ready.png` 证明报告页主操作是“转发这份报告”，PDF 为次操作。该检查证明作者端当前分享卡配置范围正确，不证明消息已送达，也不替代第二微信账号打开和撤销后重试。
- 同一报告为 0 条问题，但旧标注渲染图中保留编号 `2`，导致“0 项问题”与照片标记互相矛盾。现统一规则：照片组有已确认问题时显示标注图；零问题时优先显示原图，避免孤立编号。标注几何、渲染图和历史快照仍保留，不删除数据。
- 在线报告运行态已确认问题数为 0 且展示路径等于原图路径，修复后截图为 `18-zero-issue-original-photo.png`。PDF 分组使用同一规则，并新增“有问题显示标注图／零问题显示原图”回归。
- 全量自动回归 `43/43`、WXML 26 个、WXSS 29 个、JavaScript／JSON／原生模板及补丁空白检查通过；离线 PDF 重新生成并验证 3 个照片组、同图双问题、零问题原图和长文字分页。

## 墨绿撤回与字体职责最终锁定（2026-09-23 00:55）

- 用户明确否定品牌图形内部继续残留墨绿后，v8 几何标记由“深松石＋朱砂”改为“中性墨＋朱砂”；界面、LOGO 与应用图标现在共用纸白 `#F5F1E9`、中性墨 `#1C1A17`、朱砂 `#E15D3A`，不再保留第三套墨绿色相。

## 正式界面再收敛（2026-09-23 02:14）

- 再次以微信开发者工具正式运行态核对项目列表、项目详情、现场记录、精准标注、单份报告和“我的”。页面底色为暖纸 `#F5F1E9`，未发现墨绿色页面、导航或卡片底色；标注页深色区域仅为中性炭黑照片工作区。
- 字体职责进一步收窄：页面标题、项目名、采集标题、核对分组、列表与普通报告分组全部统一为系统无衬线；展示宋体只留给“毫厘智管”品牌字与单份报告封面项目标题。
- 辅助文字下限由 `24rpx` 提升到 `26rpx`，非激活文字由 `#77766F` 加深为 `#706D67`；小字号朱砂文本使用 `#B83F27`，主按钮与标注线继续使用 `#E15D3A`。

## 确认稿纸色与全局字体再收敛（2026-09-23 05:20）

- 前述阶段性色值已被本节取代。最终运行值为暖纸 `#E9E4DD`、输入面 `#F2EEE8`、中性墨 `#191816`、朱橙 `#DE6E3F`、分隔 `#C9C1B7`、照片留白 `#DDD6CC`。
- v8 Logo、应用图标、正式 WXSS、原生底栏、标注画布、交互母版和 PDF 模板已经同步；项目、报告、我的、现场记录和精准标注均重新从微信开发者工具运行态截图。
- 字体只保留两种职责：含中文的对象标题与报告封面使用宋体；导航、按钮、列表、表单、说明、数字及纯英文／编号标题使用苹方或系统黑体。底栏三个图标统一为相同线宽的轮廓图标，当前项用墨黑＋朱橙短线，不使用墨绿或实心块。
- 运行态证据与并排对照见项目根目录 `design-qa.md`。46 项自动测试、26 个 WXML、29 个 WXSS、JavaScript／JSON／原生模板检查及三档宽度母版检查通过。
- 正式页面截图和逐页结论见 `output/product-audit-2026-09-23-run2/audit.md`。本轮 44 项自动测试、原生模板与样式编译、离线 PDF 版式均通过。
- 底栏运行态补查发现真实缺陷：自定义组件 `selected` 在三个一级页均可能为空，外观会像“没有当前页”或“几个都在高亮”。项目、报告、我的现在都在 `onShow` 主动同步当前路由；开发者工具连续切换实测依次得到对应的 selected 路径。当前图标改为实心墨色，非当前保持灰色线框。
- 报告列表去掉每行重复的“巡查报告”后缀，仅显示项目名；页面级“报告”标题已经提供语境。修复后全量自动测试更新为 45/45，通过原生模板与样式编译和离线 PDF 复核。
- 字体职责进一步收窄：展示宋体 `500` 只用于“毫厘智管”品牌字与单份报告封面项目标题；页面标题、项目／报告列表标题、问题分组、用户姓名、公司名称、正文、表单、按钮、导航、编号与时间全部使用同一系统黑体。运行态计算样式确认普通界面标题为系统黑体 `600–650`。
- 精准标注页保留中性炭黑照片工作区，用于辨别横竖图边界、留白和标注对比；工具栏、导航和页面背景仍为暖纸，不把工作区误扩散成主题底色。
- 压力草稿 `inspection-create-qa20-20260923` 此前只删了错误账号索引，本轮重新读取当前微信账号存储后发现它仍存在；现已精确删除该草稿与当前账号索引项，并验证项目详情只剩原草稿 `inspection-create-muctvi9y-8hbxyfwc2h`，未影响历史记录与报告。
- 正式运行审查截图与逐步记录位于 `output/product-audit-2026-09-23/`。项目、报告、我的重新取图后均显示墨色品牌标记；项目详情截图已覆盖为清理后的单草稿状态。

## 依据最终确认稿重新对齐（2026-09-23 03:13）

- 用户再次以最终确认稿指出：问题不是十六进制色值是否名义上属于绿色，而是正式页面仍缺少确认稿的照片主体、宋体对象标题和暖纸连续界面，标注页整块深色幕布也会继续产生旧墨绿方案的感知。此前“普通界面全部改为无衬线”的结论作废。
- 字体职责按确认稿重置为：品牌名、项目名称、照片标题、项目记录进度及报告封面项目名使用展示宋体 `500`；页面标题、导航、按钮、表单、正文、数字、时间、用户和公司资料使用系统黑体。项目列表与报告列表展示的是项目对象，因此项目名同样使用展示宋体。
- 项目详情正式实现已重排为“当前项目 → 最近现场照片 → 图片说明 → 记录进度 → 单一主操作”。最近照片来自该项目相册真实数据，可点击预览；没有照片时不放占位图。记录／照片／报告入口移到主操作后，避免首屏再次变成目录。
- 色彩收敛为暖纸 `#F6F1E8`、中性墨 `#191816`、朱橙 `#E15D3A`。精准标注的照片留白由整块深色幕布改为暖灰 `#E8E1D7`，状态栏和工具栏继续使用暖纸；正式小程序、交互母版和 PDF 同步更新。
- 现场采集阶段将可为零问题的图片分组由“问题 一”改为“照片 一”，避免在人工判断前预设缺陷；核对与报告仍按真实问题数量使用“问题 一”或“现场照片 一”。
- 正式开发者工具截图：`output/runtime-visual-audit/02-project-detail-final.png`、`06-report-list-final.png`、`07-annotation-final-warm.png`。确认稿与项目详情并排及聚焦对照位于同目录；完整设计 QA 为项目根目录 `design-qa.md`，结果为 `passed`。
- 全量自动回归 `45/45`、WXML 26 个、WXSS 29 个、JavaScript／JSON／原生模板检查和补丁空白检查通过。模拟器只证明当前 iPhone 15 Pro Max 运行态；Android 宋体回退、真机触控及安全区仍未验证。

## 项目到核对的连续点击复核（2026-09-23 03:41）

- 在正式开发者工具中，从项目列表进入 `QA-0922-B`，点击项目详情唯一主操作“继续这次记录”，恢复的项目 ID、会话 ID、照片 ID 分别为 `project-50768a216f7e6b9f972d132a2b1df6d4fbc21643`、`inspection-create-muctvi9y-8hbxyfwc2h`、`visual-audit-photo`；进入标注、保存返回、进入人工核对、返回采集及再次打开项目后均保持一致。
- 首次连续点击暴露真实竞态：照片已显示但内部解码状态尚未 ready 时，“保存标注并返回”仍可点击并弹出“图片未就绪”。现在编辑器向页面明确上报 ready／error，加载期间按钮禁用并显示“照片加载中…”，重复图片加载只允许最新请求更新状态；修复后同一链路实际保存得到持久标注图并返回采集。
- 项目列表与人工核对页分别滚到最底部做运行测量，最后项目行和最后照片问题组均完整位于固定主操作上方；顶部导航背景由 96% 透明暖纸改为完全不透明暖纸，滚动内容不再在导航区域形成残影。
- 运行证据位于 `output/runtime-chain-audit/`，8 项精确断言见 `result.json`；本轮没有提交草稿、创建新报告、调用 AI／语音／公网 PDF 或发送分享。

## 首次使用、报告资料与单报告分享复核（2026-09-23 03:54）

- 正式运行态依次打开欢迎页、隐私与服务说明、“我的”、报告资料和一份隔离报告。欢迎页说明微信身份、无需手机号及资料可后补；隐私说明在开始使用前可达；“我的”读取有效 OpenID，身份初始化失败不会展示成正常用户。
- 报告资料页同时包含巡查人姓名／电话、公司名称／电话／地址和公司 LOGO；从“我的”定向进入“巡查人”后，动态按状态栏与胶囊高度预留顶部空间，不再把标题压进固定导航。欢迎页也删除了顶部与正文重复出现的产品名。
- 隔离报告分享配置实际返回的查询字段只有 `reportId`、`shareToken`，指向单份只读报告；没有项目 ID、会话 ID、内部任务或报告列表参数。本轮没有真正发送消息，也不能替代第二微信账号打开及撤销验证。
- 运行证据位于 `output/runtime-account-delivery-audit/`，5 项断言见 `result.json`。

## 项目入口与零问题报告语义收敛（2026-09-23 01:10）

- 项目详情原来的主按钮写“继续现场记录”，点击后却再次弹出“新建／继续”选择；同时下方草稿行又能继续，形成三处重复决策。现改为：有草稿时主按钮“继续这次记录”直接恢复最近草稿；“新建记录”作为未完成记录标题旁的明确次操作；具体草稿行继续对应会话。旧草稿不会因新建而删除。
- 在正式开发者工具中点击主按钮后，实际进入 `pages/inspection/create/index`，恢复 `QA-0922-B` 的 `inspection-create-muctvi9y-8hbxyfwc2h`，项目 ID、会话 ID 和 1 张照片一致。更新后的项目详情截图为 `output/product-audit-2026-09-23/02-project-detail.png`。
- 零问题照片不再显示成“问题 一 / 0 项”：人工核对、在线报告和 PDF 统一写为“现场照片 一／未记录问题”；照片一旦有关联问题，仍严格保留已确认的“问题 一 / 1. / 2.”编号，不改历史问题顺序。
- 报告一级页原有标题栏“筛选”和下方“选择项目”两个相同入口；已删除前者，只保留会显示“全部项目／当前项目”的筛选行。小程序配置中的系统兜底 Tab 图标也由旧 PNG 资源换成与自定义三栏相同的项目、报告、我的线性图标，避免加载切换时闪回另一套视觉。v8 以前的 19 个品牌探索资产从正式资源目录移到 `docs/brand/archive/`；正式包内只保留 v8 墨色／朱砂资产。
- 全量自动回归更新为 `44/44` 通过；WXML 26 个、WXSS 29 个、JavaScript／JSON／原生模板及补丁空白检查通过；离线 PDF 再次验证 3 个照片组、同图双问题、零问题照片和长文字分页。
